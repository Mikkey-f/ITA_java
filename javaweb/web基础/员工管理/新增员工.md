

# 新增员工

<img src="C:\Users\24709\AppData\Roaming\Typora\typora-user-images\image-20250616214610033.png" alt="image-20250616214610033" style="zoom:50%;" />

```mysql
-- 新增员工信息
-- 保存员工基本信息-emp
insert into emp(username, name, gender, phone, job, salary, image, entry_date, dept_id, create_time, update_time) VALUES ('szj','司志俊',1,'13881542277',1,6000,'1.jpg','2020-01-01',1,'2020-01-01 00:00:00','2025-06-09 00:00:00');

-- 批量保存员工工作经历信息
insert into emp_expr(emp_id, begin, end, company, job)
     VALUES (37,'2020-01-01','2021-01-01','百度','java开发'),(37,'2021-01-01','2023-01-01','字节','java开发');
```



![](C:\Users\24709\AppData\Roaming\Typora\typora-user-images\image-20250616221425682.png)

![image-20250616223151823](C:\Users\24709\AppData\Roaming\Typora\typora-user-images\image-20250616223151823.png)



## 批量保存员工工作经历：

![image-20250617133209365](C:\Users\24709\AppData\Roaming\Typora\typora-user-images\image-20250617133209365.png)

```java
@Options(useGeneratedKeys = true, keyProperty = "id") // 获取新增数据的主键值
    //useGeneratedKeys = true 表示使用数据库自动生成的主键值 keyProperty = "id" 表示将主键值保存到emp对象的id属性中
    @Insert("insert into emp(username, name, gender, phone, job, salary, image, entry_date, dept_id, create_time, update_time) " +
            "VALUES (#{username}, #{name}, #{gender}, #{phone}, #{job}, #{salary}, #{image}, #{entryDate}, #{deptId}, #{createTime}, #{updateTime})")
    void insert(Emp emp);
```

```java
@Override
    public void save(Emp emp) {
        //1。保存员工基本信息
        emp.setCreateTime(LocalDateTime.now());
        emp.setUpdateTime(LocalDateTime.now());
        empMapper.insert(emp); //e

        //2. 保存员工工作经历信息
        List<EmpExpr> exprList = emp.getExprList();
        if (!CollectionUtils.isEmpty(exprList)){
            //遍历集合，为每个员工工作经历设置员工id empId
            exprList.forEach(empExpr -> {
                empExpr.setEmpId(emp.getId());
            });

//            for (EmpExpr expr : exprList) {
//                expr.setEmpId(emp.getId());
//            }
            empExprMapper.insertBatch(exprList);
        }
    }


<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.szj.mapper.EmpExprMapper">

    <!--批量保存员工工作经历信息
    foreach 标签:
        collection: 遍历的集合的名字
        item: 遍历出来的元素
        separator: 每次循环之间的分隔符
    -->
    <insert id="insertBatch">
        insert into emp_expr(emp_id, begin, end, company, job) values
        <foreach collection="exprList" item="expr" separator=",">
            (#{expr.empId},#{expr.begin},#{expr.end},#{expr.company},#{expr.job})
        </foreach>
    </insert>

</mapper>
```

<img src="C:\Users\24709\AppData\Roaming\Typora\typora-user-images\image-20250617141350700.png" alt="image-20250617141350700" style="zoom: 67%;" />

