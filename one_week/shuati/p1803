https://www.luogu.com.cn/problem/P1803

#include<bits/stdc++.h> 
using namespace std;
int n;
struct ss{
	int a;
	int b;
}a[100010];
bool cmp(ss a,ss b){
	return a.b<b.b;
}//以结束时间为标准从小到大排序
/*为什么不选择开始时间排序呢；如果一场比赛的开始时间很早但持续时间过长，就导致能参加的比赛就会很少； 
但如果选择结束时间来排序，开始时间是确定的，就能使参加的比赛更多。*/ 
int main(){
	cin>>n;
	for(int i=1;i<=n;i++){
			cin>>a[i].a>>a[i].b;
		}
	sort(a+1,a+n+1,cmp);//结构体排序 
	int k=1,c=a[1].b;/*C记录要参加的比赛的结束时间 ；将C初始化为第一个比赛的结束时间；第一个比赛是一定会参加的，
	将K初始化为1。*/ 
	for(int i=2;i<=n;i++){
		if(a[i].a>=c){
			k++;
			c=a[i].b;//将C更新为当前比赛的结束时间。 
		}
	}
	cout<<k;
	return 0;
}
