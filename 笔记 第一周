4.24
.java 文件：它是 Java 源代码文件，由开发者使用 Java 语言编写
.class 文件：这是 Java 字节码文件，是由 javac 编译器将 .java 文件编译后生成的，文件内容是二进制形式的字节码，这些字节码是一种中间语言，由一系列的字节指令组成，不是人类直接可读的文本，而是供 Java 虚拟机（JVM）理解和执行的代码。
.java文件通过javac（java语言编译器）编译后变成.class文件， java 命令启动 JVM 加载 .class 文件。
Javac Hello.java   java Hello

4.25
数据类型

float它大约能表示 7 位有效数字
double 15-16位

 数组
1.声明数组变量
1.一维数组：int[] arr;
2.二维数组：int[][] arr;
2.动态初始化数组（要用 new ，Java 是纯面向对象语言，所有对象（包括数组）都在堆内存中分配空间。new 关键字用于在堆上为对象（数组）分配内存空间，同时调用相应的构造函数（数组有隐式的初始化过程 ））
1.一维：int[] arr = new int[5];
2.二维：int[][] arr = new int[5][5];
3.不规则数组：
3.
int[][] arr = new int[3][];
// 为外层数组的每个元素（即内层数组）分配不同的长度
arr[0] = new int[2];
arr[1] = new int[3];
arr[2] = new int[4];
3.静态初始化数组
1.一维：
1.int[] arr = {1, 2, 3};
2.或 int[] arr = new int[]{1, 2, 3};
2.二维：int[][] arr = {{1, 2}, {3, 4}, {5, 6}};
基本数据类型->string
+“”
int a=10;
String s=a+” ”;



4.26
命名规范

4.方法调用机制

 Java 虚拟机（JVM）中，栈空间用于存储方法调用时的信息，如局部变量、操作数栈、动态链接信息以及返回地址等 


当方法调用完之后，为该方法开辟的栈空间会被释放
5.方法调用细节 
一个类里面可以有多个方法，但是方法里面不能再定义方法 
同一个内部类里面的一个方法可以被另一个方法直接调用，不需要定义一个类之后再调用
静态内部类之间，各自的方法可以相互调用，非静态内部类之间的方法要通过实例调用
外部类中，外部类的非静态方法之间可以相互直接调用。
外部类的静态方法之间也可以相互直接调用
如果要在静态方法中调用非静态方法，必须先创建类的实例，然后通过实例来调用
外部类的非静态方法可以直接调用静态方法，也可以通过类名调用






java里面的参数传递
Java 中一切参数传递都是值传递，对于基本类型（int float等），传递的是数据值本身的副本；对于引用类型（类的实例，数组都属于引用类型 ），传递的是对象引用（内存地址）的副本。

把对象作为参数传入方法时，就好比把记录这个人住址（对象在堆内存中的地址）的小纸条复制了一份给方法内的形参。main 方法里的原始引用和方法里的形参引用，都通过这张小纸条（地址）找到堆内存里同一个 “人”（对象） 。


4.26
Person p = new Person();
p存储堆中 Person 对象的内存地址，指向创建的person对象
在 Java 中，方法返回对象时，实际上返回的是该对象在堆内存中的地址（也就是对象的引用 ） 。


字段=属性=成员变量 他们三个都是同一个东西


重载  
一个类里面 同名 不同参数（参数个数不同，参数类型不同，参数类型顺序不同 都叫不同参数） 的方法
如果是同名同参，返回值类型不同的方法，不构成参数重载，而且这种写法是错误的，会报错，因为是方法的重复定义

可变参数


可变参数的本质就是数组

   








Arrays 类
是 Java 中 java.util 包下的一个类。这个类提供了大量静态方法，这些方法可以用于数组的常见操作
在 Java 里，数组有一个内置的 length 属性，你可以通过 .length 来获取数组的元素个数。

Arrays类
常用方法
排序  Arrays.sort()  
二分查找Arrays.binarySearch()
复制   Arrays.copyOf()
填充方法Arrays.fill()
比较方法Arrays.equals()：




对象引用：在 Java 里，像 p1 和 p2 这样的变量是对象引用，它们存储的是对象在堆内存中的地址。当执行 p1 = p2 时，只是让 p1 存储的地址和 p2 一样，也就是让它们指向了同一个对象实体,，p1对对象属性进行修改会改变p2的对象属性，因为他们指向同一个对象。
对象复制：对象复制的目的是创建一个新的对象，这个新对象的属性值和原对象相同，但它们在内存中是相互独立的两个实体。修改其中一个对象的属性，不会影响到另一个对象。
所以不能用p2=p1来进行复制

作用域
Java里全局变量就是属性，局部变量是方法里的变量，全局变量可以不初始化，因为有默认值，局部变量必须初始化






构造器
对象创建流程分析




this
this本质就是一个这个对象的另一个引用，每个对象都有一个this
this就是代表这个对象 
比如  p1.compareTo(p2);

包 

包的本质就是创建不同的文件夹/目录来保存类文件



java.lang 包中的类可直接使用，但 方法 要通过类名（Math类里面的）或对象（string类里面的）来调用。


如果一个类里要使用和自己在相同包下的类时不需要额外导入，可以直接调用，
若要使用和自己在不同包中的类，就必须使用 import 语句导入该类









在一个 Java 源文件（.java 文件）中，最多只能有一个 public 外部类，并且该类的名称必须与文件名相同（包括大小写）。但是可以有多个非public的外部类


访问修饰符


