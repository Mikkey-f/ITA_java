5.1

多态
1.动态绑定机制
 一层一层往上找的那个调用规则
2.多态数组
定义类型为父类型，里面保存的实际元素的类型为子类型
3.多态参数 
当一个方法的形参类型为父类时，由于子类是父类的一种特殊形式（通过继承关系），所以可以将子类对象作为实参传递给该方法。这就是利用了向上转型，即把子类对象赋值给父类引用，此时父类引用就可以指向子类对象，并且在调用方法时，会根据对象的实际类型调用相应的重写方法（动态绑定）。
4.字符串拼接机制
在 Java 中，当使用 + 运算符连接不同类型的数据时，如果其中至少有一个操作数是 String 类型，那么 Java 会自动将其他操作数转换为 String 类型，然后进行字符串拼接，最终得到一个新的 String 对象。
经常用来用来return一条有基本数据类型的句子，返回值是String

5.字面量
因为在 Java 里，字面量的类型是有默认规则的
整数类型的字面量默认是 int 类型。
小数类型的字面量默认是 double 类型。
所以我们总是会不注意地进行隐式类型转换，只能低精度类型转向高精度类型
6.虽然在同一个类中，非静态方法可以相互直接调用，但在 main 方法这种特殊的静态环境中，只能直接调用静态方法，这就是为什么这里要将 showEmpAnual 和 testWork 方法定义为 static 的原因或者创建一个本类的对象来调用。


5.2
1.Object类
Object 类是所有类的基类，也就是所有类都直接或间接地继承自 Object 类。


equals方法
如果要判断内容是不是相等，基本数据类型（int，double，float）用==，对象（String，interge，Person）用equals
在父类object里equals方法默认就是判断引用类型是否相等（引用类型存储的是对象在内存中的地址），也就是只能判断是不是指向同一个对象，存储的地址是否相同，但是object的子类往往会重写equals方法，比如String，Interge用equals方法判断值两个的值是否相等
== 运算符用于比较两个对象的引用是否相等，也就是判断两个变量是否指向内存中的同一个对象，而不是比较对象的内容。String 是引用类型，当使用 == 比较两个 String 对象时，它比较的是这两个对象在内存中的地址，而非字符串的实际内容。我们应该使用 equals()，方法来比较 String 对象的内容
equals 方法是定义在 java.lang.Object 类中的一个实例方法，用于比较两个对象的内容是否相等，它的参数是 Object 类型。对于基本数据类型（如 byte、short、int、long、float、double、char、boolean），它们不是对象，没有继承 Object 类，因此没有 equals 方法可供调用

hashCode方法
是不是指向同一个对象


Finalize方法（应付面试）






2.断点调试（debug）






5.3
实体类（模型类）用于抽象实体 domain
工具类（排序，算法）专注功能实现
主类（有main方法）把控程序执行流程



5.4
房屋出租系统


5.5
static
类变量（静态属性）
静态变量被所有对象所共有
静态变量随着类加载而创建的，所以不需要实例
静态变量可以直接通过类名来访问 用.
但也要满足访问权限

类方法（静态方法）
静态方法可以直接通过类名来调用
静态方法可以访问静态变量

类方法和普通方法都是随着类的加载而加载，将结构信息存储在方法区


Main

代码块
可以理解为只有方法体的方法，但是他不是方法




细节：（面试）

普通代码块可以理解为构造器的补充，普通代码块和构造器只有new了才会执行

、






单例模式（只有一个类的实例，程序里面只能出现一个这个类的对象（重量级），不能是多个 单例）
1.饿汉式（类一加载就有对象）
不调用方法也已经有一个对象了，只要类加载堆里面就有对象，而且因为他是静态的所以只会在类加载时new一次，满足单例

2.懒汉式（调用方法才有对象）
你调用方法我才给你一个对象，因为静态属性它是静态，所以只会有一个cat，加上getInstance这个方法里面的if语句，满足了单例


没new就没有创建一个对象只是声明，new了才是在堆里创建了一个对象










5.6
final关键字（修饰的变量又叫常量）常量
不准修改它！不准继承它（类）！不准重写它（父类里面的方法）！！！









抽象类abstract




接口 interface
接口的本质
o接口是一种抽象的规范 / 契约，定义了一组方法声明（只有方法名、参数、返回值类型，没有具体实现）。
类实现接口
o类通过 implements 关键字承诺遵守接口的规则，并必须实现接口中的所有方法。
1.接口实现关系：类 B 和类 C 都实现了接口 A，因此它们的实例属于接口 A 的 “子类型”。
2.父类引用指向子类对象：方法参数声明为接口 A 类型时，实际可以接收所有实现了该接口的类的实例（如 B、C），这符合多态的规则。
3.运行时动态绑定：传入 B 或 C 的实例后，方法会根据实例的实际类型（B 或 C）调用其重写的接口方法。



