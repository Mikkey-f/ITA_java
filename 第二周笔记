5.1

多态
1.动态绑定机制
 一层一层往上找的那个调用规则
2.多态数组
定义类型为父类型，里面保存的实际元素的类型为子类型
3.多态参数 
当一个方法的形参类型为父类时，由于子类是父类的一种特殊形式（通过继承关系），所以可以将子类对象作为实参传递给该方法。这就是利用了向上转型，即把子类对象赋值给父类引用，此时父类引用就可以指向子类对象，并且在调用方法时，会根据对象的实际类型调用相应的重写方法（动态绑定）。
4.字符串拼接机制
在 Java 中，当使用 + 运算符连接不同类型的数据时，如果其中至少有一个操作数是 String 类型，那么 Java 会自动将其他操作数转换为 String 类型，然后进行字符串拼接，最终得到一个新的 String 对象。
经常用来用来return一条有基本数据类型的句子，返回值是String

5.字面量
因为在 Java 里，字面量的类型是有默认规则的
整数类型的字面量默认是 int 类型。
小数类型的字面量默认是 double 类型。
所以我们总是会不注意地进行隐式类型转换，只能低精度类型转向高精度类型
6.虽然在同一个类中，非静态方法可以相互直接调用，但在 main 方法这种特殊的静态环境中，只能直接调用静态方法，这就是为什么这里要将 showEmpAnual 和 testWork 方法定义为 static 的原因或者创建一个本类的对象来调用。


5.2
1.Object类
Object 类是所有类的基类，也就是所有类都直接或间接地继承自 Object 类。


equals方法
如果要判断内容是不是相等，基本数据类型（int，double，float）用==，对象（String，interge，Person）用equals
在父类object里equals方法默认就是判断引用类型是否相等（引用类型存储的是对象在内存中的地址），也就是只能判断是不是指向同一个对象，存储的地址是否相同，但是object的子类往往会重写equals方法，比如String，Interge用equals方法判断值两个的值是否相等
== 运算符用于比较两个对象的引用是否相等，也就是判断两个变量是否指向内存中的同一个对象，而不是比较对象的内容。String 是引用类型，当使用 == 比较两个 String 对象时，它比较的是这两个对象在内存中的地址，而非字符串的实际内容。我们应该使用 equals()，方法来比较 String 对象的内容
equals 方法是定义在 java.lang.Object 类中的一个实例方法，用于比较两个对象的内容是否相等，它的参数是 Object 类型。对于基本数据类型（如 byte、short、int、long、float、double、char、boolean），它们不是对象，没有继承 Object 类，因此没有 equals 方法可供调用

hashCode方法
是不是指向同一个对象


Finalize方法（应付面试）






2.断点调试（debug）






5.3
实体类（模型类）用于抽象实体 domain
工具类（排序，算法）专注功能实现
主类（有main方法）把控程序执行流程



5.4
房屋出租系统


5.5
static
类变量（静态属性）
静态变量被所有对象所共有
静态变量随着类加载而创建的，所以不需要实例
静态变量可以直接通过类名来访问 用.
但也要满足访问权限

类方法（静态方法）
静态方法可以直接通过类名来调用
静态方法可以访问静态变量

类方法和普通方法都是随着类的加载而加载，将结构信息存储在方法区


Main

代码块
可以理解为只有方法体的方法，但是他不是方法




细节：（面试）

普通代码块可以理解为构造器的补充，普通代码块和构造器只有new了才会执行

、






单例模式（只有一个类的实例，程序里面只能出现一个这个类的对象（重量级），不能是多个 单例）
1.饿汉式（类一加载就有对象）
不调用方法也已经有一个对象了，只要类加载堆里面就有对象，而且因为他是静态的所以只会在类加载时new一次，满足单例

2.懒汉式（调用方法才有对象）
你调用方法我才给你一个对象，因为静态属性它是静态，所以只会有一个cat，加上getInstance这个方法里面的if语句，满足了单例


没new就没有创建一个对象只是声明，new了才是在堆里创建了一个对象










5.6
final关键字（修饰的变量又叫常量）常量
不准修改它！不准继承它（类）！不准重写它（父类里面的方法）！！！









抽象类abstract




接口 interface
接口的本质
o接口是一种抽象的规范 / 契约，定义了一组方法声明（只有方法名、参数、返回值类型，没有具体实现）。
类实现接口
o类通过 implements 关键字承诺遵守接口的规则，并必须实现接口中的所有方法。
1.接口实现关系：类 B 和类 C 都实现了接口 A，因此它们的实例属于接口 A 的 “子类型”。
2.父类引用指向子类对象：方法参数声明为接口 A 类型时，实际可以接收所有实现了该接口的类的实例（如 B、C），这符合多态的规则。
3.运行时动态绑定：传入 B 或 C 的实例后，方法会根据实例的实际类型（B 或 C）调用其重写的接口方法。


类的五大成员
属性 方法 构造器 代码块 内部类

5.7 
内部类 InnerClass

局部内部类 AnonymousInnerClass

它就是在方法里面创一个类拿来用一用从而可以完成，写完这个方法而已，就是局部变量啊
和c里面的函数里面的定义一些东西来完成这个函数一样，只不过是这里是方法里面需要定义类才能完成这个方法






匿名内部类

基于类的匿名内部类
定义与本质：是没有类名的内部类，本质上是某个类（可以是抽象类或具体类 ）的子类。它在定义的同时会创建一个该子类的实例对象。
语法与示例：语法形式为new 父类(构造参数) { // 重写父类方法或添加新方法 } 
基于接口的匿名内部类
定义与本质：是没有类名的内部类，是某个接口的实现类，在定义时会创建该实现类的实例对象。
语法与示例：语法形式为new 接口() { // 实现接口中的抽象方法 } 


有这种匿名内部类就可以不用创建具体的实现接口的类或者是类的不同的子类


成员内部类
就是一个类里面有另一个类，这个类是它的属性，它的低位就是一个成员，就是一个属性啊




静态内部类
就是加一个static的成员内部类，加了static就不能访问非静态变量



外部类内访问成员内部类的成员：创建内部类的实例，而且外部类可以访问内部类的私有成员
1.外部类其他类访问成员内部类的成员：通过外部类的实例来创建
外部类名.内部类名 变量名=外部类实例名.new 内部类构造方法();
//（1）. 先创建外部类实例
    Outer outer = new Outer();
    
    //（2） 通过外部类实例创建内部类实例
Outer.Inner inner = outer.new Inner();//把new Inner()看做outer的一个属性




2.外部其他类访问静态成员内部类：  
（1）创建静态内部类的实例
外部类名.静态内部类名 变量名 = new 外部类名.静态内部类名(构造参数);
（2）访问静态内部类的静态成员
外部类名.静态内部类名.静态成员;

1.如果内部类有成员和外部类重名，要用 外部类名.this.成员  来访问
2.静态内部类  外部类名.成员来访问  因为可以在静态内部类里面访问的成员一定也是静态的，所以可以直接通过类名来访问，不用.this这个对象来访问
















5.9
枚举 enumeration


enum关键字 枚举类 enum代替class

Season[] seasons=Season.values();
for(Season season:seasons){//增强for循环
    System.out.println(season);
}

5.10.2025






异常Exception（运行时异常，编译异常）


所有异常都是类，它们通过继承形成层次结构。
受检查异常必须显式处理，而运行时异常可以选择性处理。
** 错误（Error）** 通常表示系统级问题，程序不应尝试捕获。

异常体系图



处理异常的方法：
try-catch


异常最终没有被抛出就是还可以继续运行，被抛出后异常后的代码不能再被执行


5.11
为什么需要传递System.in？
Scanner类本身不会直接与键盘或文件交互，它需要一个数据源来读取数据。通过传递System.in，你告诉Scanner：“我要从标准输入（键盘）读取数据”。
scanner.nextLine(); 的作用是清除输入缓冲区中的错误内容
在 Java 中Integer是整数的包装类，用于将基本数据类型int封装为对象。Java 为每个基本数据类型都提供了对应的包装类，Integer就是int的包装类。



包装类 Wrapper




valueOf() 方法接收一个 基本类型/包装类/类的参数，返回其对应的类对象
toString()方法 包装类转字符串
parseInt()方法 参数（String等）转成基本数据类型然后基本数据类型可以自动装箱

常用类和方法



